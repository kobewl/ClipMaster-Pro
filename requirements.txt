# ClipMaster Pro Dependencies
# Python 3.10+ recommended

# GUI framework
PyQt6>=6.4.0
PyQt6-Qt6>=6.4.0

# Windows integration
pywin32>=305; platform_system=="Windows"

# Global keyboard monitoring
# - Windows: required for global hotkeys fallback and AI typing monitor
# - macOS:   pynput is used instead (keyboard needs root on macOS)
keyboard>=0.13.5; platform_system=="Windows"

# macOS native window helpers (window level / topmost)
pyobjc-framework-Cocoa>=9.0; platform_system=="Darwin"

# Process information for source tracking
psutil>=5.9.0
pynput>=1.7.7

# ── Optional AI packages ───────────────────────────────────────────
# Base app startup does not require LangChain.
# Install these only if you want richer AI provider integrations.
# langchain>=0.2.0
# langchain-core>=0.2.0
# langchain-openai>=0.1.0          # OpenAI + any custom OpenAI-compatible endpoint

# ── Optional provider packages (install the ones you need) ──
# pip install langchain-anthropic      # Anthropic Claude
# pip install langchain-google-genai   # Google Gemini
# pip install langchain-ollama         # Ollama (local models)
# pip install langchain-deepseek       # DeepSeek

