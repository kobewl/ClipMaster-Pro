# ClipMaster Pro Dependencies
# Python 3.10+ recommended

# GUI framework
PyQt6>=6.4.0
PyQt6-Qt6>=6.4.0

# Windows integration
pywin32>=305; platform_system=="Windows"

# Process information for source tracking
psutil>=5.9.0
pynput>=1.7.7

# ── Optional AI packages ───────────────────────────────────────────
# Base app startup does not require LangChain.
# Install these only if you want richer AI provider integrations.
# keyboard>=0.13.5              # Global hotkeys / AI typing monitor on macOS needs Accessibility permission
# langchain>=1.2.0
# langchain-core>=1.2.0
# langchain-openai>=1.1.0          # OpenAI + any custom OpenAI-compatible endpoint

# ── Optional provider packages (install the ones you need) ──
# pip install langchain-anthropic      # Anthropic Claude
# pip install langchain-google-genai   # Google Gemini
# pip install langchain-ollama         # Ollama (local models)
# pip install langchain-deepseek       # DeepSeek
